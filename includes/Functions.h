#ifndef FUNCTION_H
#define FUNCTION_H

#include <vector>
#include <string>

std::vector <std::string> wordSeparator(std::string );

void lowerStr(std::string &);

std::string bringImportant(const std::string& , size_t = 0 ); //sugest to pass argument for delete

std::string vecToStr (std::vector<std::string>, size_t );

#endif